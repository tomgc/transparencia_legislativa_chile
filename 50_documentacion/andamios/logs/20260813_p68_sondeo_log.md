# Bitácora — Sondeo P-68 de fuentes temáticas (LeyChile y `datos.bcn.cl`)

> Sesión 20, 2026-08-13. Encargo:
> `50_documentacion/andamios/50_encargo_p68_sondeo_leychile_bcn.md`.
> Rama: `sondeo/p68-fuentes-tematicas`. Reproductor:
> `20_insumos/exploracion/20260813/20260813_sondeo_p68.R` (la carpeta
> `20_insumos/exploracion/` está gitignored por `.gitignore:57`, igual que el
> precedente `20260807/`: el reproductor vive en disco, no en el índice).
>
> **Estado: detenido tras G3 por instrucción del titular.** G4 no se ha
> ejecutado. Contador de red: **2 de 500**.

---

## Estado de las compuertas

| Compuerta | Estado | Resumen |
|---|---|---|
| **G0** — estado del repositorio | **PASA** (en segundo intento) | Bloqueó en el primer intento por 3 de 4 comprobaciones; resuelto con las tres disposiciones autorizadas por el titular |
| **G1** — universo contado hoy | **CUMPLE CON DESVIACIÓN DECLARADA** | Fusible armado, exit 0 (no se disparó). Fuente sustituida (ver abajo). El universo **no** es el del veredicto vigente: 427 boletines, no 381 |
| **G2** — ¿responde `datos.bcn.cl`? | **PASA — rama ABIERTA** | 1 llamada, HTTP 200, resultado SPARQL bien formado |
| **G3** — ¿responde LeyChile? | **PASA — rama ABIERTA** | 1 llamada, HTTP 200, XML de norma con raíz `Norma` |
| **G4** — techo de LeyChile | no ejecutada | — |
| **G5** — control positivo y negativo | no ejecutada | — |

---

## Ajustes que el titular mandó sobre el encargo (sesión 20, tras G1)

Se registran como enmiendas al encargo, no como interpretación del ejecutor.

1. **G1 queda CUMPLIDA CON DESVIACIÓN DECLARADA**, no como cumplimiento simple.
   La fuente fue la captura cruda del corte vigente y **no**
   `40_salidas/intermedios/proyectos_detalle.rds`, porque el intermedio está
   sellado al 2026-08-03 y regenerarlo escribe bajo `40_salidas/`, prohibido por
   §2. Justificación y control, en §G1.b y §G1.c.
2. **El universo de todo lo que sigue es 427 boletines / 422 sin materia.** Toda
   cifra posterior se declara sobre ese denominador. El 381/376 del veredicto
   vigente aparece **solo como antecedente, nunca como denominador**.
3. **Umbral de G4, corregido.** El del encargo ("menor que la cobertura que el
   portal ya tiene") quedó sin filo, porque ese piso es 1,17 %. El umbral
   operativo pasa a ser **la cohorte 2026: 336 de los 422 sin materia (79,62 %)**.
   La rama LeyChile queda cerrada si la fracción de los 422 que llegó a ser ley no
   alcanza para mover la cobertura de esa cohorte. G4 debe medir y reportar **las
   dos fracciones por separado**: sobre los 422 y sobre los 336 de 2026.

---

## G0 — Estado del repositorio

### Primer intento: BLOQUEA (3 de 4)

```
$ git status --porcelain
 M CLAUDE.md
?? 50_documentacion/andamios/50_encargo_p68_sondeo_leychile_bcn.md

$ git stash list
stash@{0}: On design/contrato-datos: wip-sesion6

$ git rev-parse --abbrev-ref HEAD
main

$ git fetch --all --prune ; git rev-parse HEAD origin/main
6f11ae82bd1843766e6f69a0b6c13057ad7ad05e   (HEAD)
6f11ae82bd1843766e6f69a0b6c13057ad7ad05e   (origin/main)
```

| # | Comprobación | Esperado | Medido | Estado |
|---|---|---|---|---|
| 1 | `status --porcelain` | vacío | 2 entradas | FALLA |
| 2 | `stash list` | vacío | 1 stash | FALLA |
| 3 | `rev-parse --abbrev-ref HEAD` | ≠ `main` | `main` | FALLA |
| 4 | `fetch` + comparación remota | al día | `HEAD == origin/main` | PASA |

**Los tres bloqueos y su naturaleza:**

1. `CLAUDE.md` modificado: entrada de "Últimos cambios" de P-59 documentando el
   PR #12, ya mergeado en `6f11ae8`. Trabajo pendiente de la sesión 19, **ajeno a
   P-68**. Dejarlo sucio contamina el criterio C9.
2. El encargo sin trackear, que declara ese mismo destino en su encabezado
   (línea 7). Si queda `??`, C9 lo cuenta como archivo fuera de ruta declarada.
3. `stash@{0}`, del 2026-07-10, sobre `design/contrato-datos`: 6 archivos de
   salida del escáner de estructura (`git stash show --stat stash@{0}`, 713
   inserciones / 2323 supresiones). Regenerable, pero **soltarlo es destructivo**.

### Disposiciones autorizadas por el titular y ejecutadas

```
$ git add CLAUDE.md ; git commit -m "docs: registrar P-59 (guarda de locale UTF-8) en CLAUDE.md"
[main 0ecb44e] docs: registrar P-59 (guarda de locale UTF-8) en CLAUDE.md
 1 file changed, 37 insertions(+), 14 deletions(-)

$ git branch wip-sesion6 stash@{0}
$ git show --stat wip-sesion6
commit fbe722816b0263824040ecf0377664f112849aec
Merge: 0473ba8 3572bb0
    On design/contrato-datos: wip-sesion6
 .../estructura/20260709_114413_estructura.md       | 409 ------------
 .../estructura/20260709_114413_estructura.txt      | 405 ------------
 .../estructura/20260710_071004_estructura.md       | 425 -------------
 .../estructura/20260710_071004_estructura.txt      | 421 -------------
 50_documentacion/estructura/estructura_actual.md   | 689 +++++++++++----------
 50_documentacion/estructura/estructura_actual.txt  | 687 ++++++++++----------
 6 files changed, 713 insertions(+), 2323 deletions(-)

$ git stash drop
Dropped refs/stash@{0} (fbe722816b0263824040ecf0377664f112849aec)

$ git switch -c sondeo/p68-fuentes-tematicas
$ git add 50_documentacion/andamios/50_encargo_p68_sondeo_leychile_bcn.md
$ git commit -m "docs(p68): encargo de sondeo de fuentes tematicas BCN"
[sondeo/p68-fuentes-tematicas e983056] docs(p68): encargo de sondeo de fuentes tematicas BCN
 1 file changed, 299 insertions(+)
```

El stash se preservó **antes** de soltarlo: la rama `wip-sesion6` mostró los 6
archivos con las mismas cifras que el stash, y solo entonces se ejecutó el
`drop`. El commit de `CLAUDE.md` quedó en `main` **sin push**: el titular decide
cuándo sube. Corresponde a P-83, ajeno a este encargo.

### Segundo intento: PASA (4 de 4)

```
$ git status --porcelain
                                      (vacío)
$ git stash list
                                      (vacío)
$ git rev-parse --abbrev-ref HEAD
sondeo/p68-fuentes-tematicas

$ git log --oneline -3
e983056 docs(p68): encargo de sondeo de fuentes tematicas BCN
0ecb44e docs: registrar P-59 (guarda de locale UTF-8) en CLAUDE.md
6f11ae8 chore(locale): guarda UTF-8 en cuatro puntos de arranque (P-59) (#12)

$ git merge-base sondeo/p68-fuentes-tematicas main
0ecb44e15a29c0f50e92e4b19995b7b50323efb3      == main
```

### Sello de inmutabilidad de `20_insumos/camara/` (base de C8)

Tomado **antes de la primera llamada de red**, en R con `tools::md5sum()`,
recursivo sobre todos los archivos. **No se vuelve a tomar**: retomarlo después
de tocar el repo destruiría su valor como control.

| Magnitud | Valor |
|---|---|
| Archivos sellados (denominador de C8) | **51** |
| Bytes totales | 1 533 938 |
| md5 del listado ordenado `<md5>  <ruta>` | **`687f75a88122472aa2f7d1b6dae9d285`** |

El detalle línea a línea (51 pares md5/ruta) quedó fuera del repo, en el
scratchpad de la sesión, para recontrastar en el cierre.

**R7 verificado por lectura:** `50_documentacion/andamios/50_fusible_red.R`
expone `instalar_fusible_red()`, traza `httr::GET`, `httr::POST`, `httr::RETRY` y
`curl::curl_fetch_memory`, y sale con `quit(save = "no", status = 99L,
runLast = FALSE)`.

---

## G1 — Universo, contado hoy y no heredado — CUMPLE CON DESVIACIÓN DECLARADA

**La desviación, enunciada antes que sus cifras.** El encargo manda medir sobre
`40_salidas/intermedios/proyectos_detalle.rds`. **No se midió sobre ese archivo.**

| Aspecto | Contenido |
|---|---|
| **Qué se desvió** | La fuente del universo: la captura cruda `20_insumos/camara/20260812_detalle_proyectos_xml_2026_tope-inf.rds`, en solo lectura, en vez del intermedio |
| **Por qué** | El intermedio está sellado al **2026-08-03** mientras `CORTE_FECHA` es **2026-08-12**. Realinearlo exige correr `regenerar_intermedios_si_desalineados()`, que **escribe bajo `40_salidas/`** — prohibido por §2 — y que se invoca desde `00_run_all.R`, que G1 prohíbe cargar. Las dos únicas salidas eran medir un universo caduco o medir la fuente de la que la guarda reconstruiría; se eligió la segunda |
| **Por qué es equivalente** | Son **los mismos bytes** que la regeneración leería: el `hash_origen` del sello apunta a la captura del corte anterior, y el mecanismo es idéntico un corte más tarde |
| **Control que la respalda** | El parser propio del sondeo reproduce a `parsear_contenido_proyecto()` en **381 de 381** boletines comunes (`n_materias` idéntico), con **0** boletines presentes en el intermedio y ausentes de la captura. Sin ese acuerdo la desviación no sería aceptable |
| **Qué NO cubre el control** | Los 46 boletines que solo existen en la captura no tienen contraparte en el intermedio: para ellos el acuerdo entre parsers no se pudo verificar, solo la ausencia de materias |
| **Riesgo residual** | Si la guarda, al regenerar, aplicara alguna transformación que el parseo directo no reproduce, el universo diferiría. Medido en lo verificable: no difiere en 381 de 381 |

Ejecutado con `Rscript 20_insumos/exploracion/20260813/20260813_sondeo_p68.R g1`,
en proceso propio que **no** carga `00_run_all.R`, con el fusible armado sobre
las 4 funciones.

**Exit code del proceso: 0.** El fusible **no** se disparó → criterio **C1
CUMPLE**. Control estructural adicional que imprime el propio reproductor:
*funciones de descarga del pipeline visibles en este proceso: **0*** (no se carga
`10_utils.R`, así que `con_cache()` y `descargar_xml_camara()` no existen aquí; la
prohibición del §2 deja de ser regla de juicio y pasa a ser imposibilidad).

### G1.a — El intermedio que nombra el encargo: DESALINEADO

| Magnitud | Valor |
|---|---|
| `CORTE_FECHA` vigente (`10_utils/10_configuracion.R:57`) | **2026-08-12** |
| Sello del intermedio, `corte_fecha` | **2026-08-03** |
| Sello, `anio_proceso` / `escrito_en` | 2026 / 2026-08-08T22:05:48 |
| Sello, `hash_origen` | `20260803_detalle_proyectos_xml_2026_tope-inf.rds` = `75a148f2a99407c64b61565a598e0435` |
| **Alineado con `CORTE_FECHA`** | **FALSE** |

Universo según ese intermedio: **381 boletines**, 381 únicos; **5 con al menos
una materia**, **376 sin ninguna**. Control interno: `n_materias` coincide con
`nrow()` del list-col `materias` en **381 de 381**.

Es la misma patología que el veredicto vigente ya documentó como riesgo 5 (sello
2026-07-27 contra `CORTE_FECHA` 2026-08-03), un refresh más tarde: los
intermedios están gitignored, el corte sí viaja, y toda copia local queda
desalineada tras cada merge del bot.

### G1.b — El universo al corte vigente, sin regenerar nada

El encargo prohíbe escribir bajo `40_salidas/`, así que **no** se corrió la guarda
`regenerar_intermedios_si_desalineados()`. En su lugar se midió directamente
sobre la captura cruda versionada de la que esa guarda reconstruiría el
intermedio — los mismos bytes, en solo lectura:

| Magnitud | Valor |
|---|---|
| Captura | `20_insumos/camara/20260812_detalle_proyectos_xml_2026_tope-inf.rds` |
| md5 | `f1a48272fe52eaf42d09f5c6afe8088f` |
| Filas | **427**, todas con `estado == "resuelto"` (427 de 427) |

El parseo es **código propio del reproductor**, no `parsear_contenido_proyecto()`:
una auditoría que reusa el parser del pipeline hereda sus puntos ciegos
(invariante 1 de `CLAUDE.md`).

**UNIVERSO DEL SONDEO, AL CORTE 2026-08-12:**

| Magnitud | Valor | Denominador |
|---|---|---|
| Boletines totales (todos únicos) | **427** | 427 filas de la captura, todas resueltas |
| Nodo `<Materias>` presente | **427** | de 427 boletines |
| **Con al menos una materia** | **5 (1,17 %)** | de **427 boletines** al corte 2026-08-12 |
| **Sin ninguna materia** ← el universo objetivo del sondeo | **422 (98,83 %)** | de **427 boletines** al corte 2026-08-12 |
| Sin `FechaIngreso` | 0 | de 427 |

R2 se reconfirma sobre el universo completo y sin red: el hueco tiene forma de
**nodo vacío**, no de nodo ausente — `<Materias>` viene presente en **427 de 427**.

### G1.b — Distribución por cohorte anual de los sin materia

Cohorte = año de `FechaIngreso` del propio XML del boletín (offline; el veredicto
vigente la obtuvo por muestreo con red sobre `retornarMocionesXAnno`).

| Cohorte | Total | Con materia | **Sin materia** | % con |
|---|---|---|---|---|
| 2016 | 3 | 3 | 0 | 100,00 |
| 2018 | 2 | 2 | 0 | 100,00 |
| 2020 | 1 | 0 | 1 | 0 |
| 2021 | 2 | 0 | 2 | 0 |
| 2022 | 6 | 0 | 6 | 0 |
| 2023 | 7 | 0 | 7 | 0 |
| 2024 | 32 | 0 | 32 | 0 |
| 2025 | 38 | 0 | 38 | 0 |
| **2026** | **336** | 0 | **336** | 0 |
| **Total** | **427** | **5** | **422** | 1,17 |

Las dos sumas cuadran contra el paso que las produjo: 422 = 422 y 427 = 427.

**Lo que esta tabla dice y el veredicto vigente no podía decir:** dentro del
universo que el portal realmente publica, la cobertura temática no es un
gradiente — es binaria y está enteramente en 2016-2018 (5 de 5 boletines
con materia), con **0 de 422 desde 2020**. Y **336 de los 422 sin materia
(79,62 %) son cohorte 2026**. Cualquier fuente alternativa que cubra "lo
legislado" y no "lo que se vota este año" choca contra ese 79,62 % antes que
contra cualquier otra cosa. Es la hipótesis B del veredicto (§4.1) vista desde el
universo completo en vez de desde una muestra.

### G1.c — Control cruzado (a) contra (b)

| Contraste | Valor |
|---|---|
| Boletines en (a), intermedio corte 2026-08-03 | 381 |
| Boletines en (b), captura corte 2026-08-12 | 427 |
| En ambos | **381** |
| Solo en (a) | **0** |
| Solo en (b) — universo nuevo del refresh | **46** |
| `n_materias` coincide en los comunes | **381 de 381** |

Dos lecturas que este control habilita:

1. **El parser propio del sondeo reproduce al del pipeline en 381 de 381
   boletines.** Sin ese acuerdo, ninguna cifra de (b) sería creíble.
2. **La serie diacrónica del veredicto se extiende:** los 46 boletines nuevos
   entre el corte 2026-08-03 y el 2026-08-12 recibieron **0 materias**. El
   veredicto había medido 0 de 64 nuevos en 28 días; ahora son **0 de 110 nuevos
   acumulados**, y el conjunto con materia sigue siendo exactamente el mismo de
   siempre.

Los 5 boletines con materia, al corte vigente:

| Boletín | Cohorte | `n_materias` | `n_votaciones` |
|---|---|---|---|
| 10634-29 | 2016 | 2 | 13 |
| 10795-33 | 2016 | 2 | 3 |
| 10986-24 | 2016 | 4 | 1 |
| 11608-09 | 2018 | 2 | 4 |
| 12234-02 | 2018 | 1 | 23 |

Son el **control positivo obligatorio de G5**: 5 boletines, `n_materias` 2, 2, 4,
2 y 1, suma 11 materias. Coinciden con los que el veredicto reportó (§4: "los
mismos 5 boletines con las mismas `n_materias` (2, 2, 4, 2, 1)").

### Cifras del veredicto vigente que este paso sustituye

| Magnitud | Veredicto (corte 2026-08-03) | **G1 (corte 2026-08-12)** |
|---|---|---|
| Boletines totales | 381 | **427** |
| Con materia | 5 (1,31 %) | **5 (1,17 %)** |
| Sin materia | 376 | **422** |

Es exactamente el caso que el encargo anticipó: la diferencia es un hallazgo del
sondeo, no un error a corregir en silencio. **El universo objetivo del sondeo son
422 boletines, no 376**, y el denominador de toda cobertura que se reporte de
aquí en adelante es 427.

---

## G2 — ¿Responde `datos.bcn.cl`? — RAMA ABIERTA

H2 no era premisa. Consulta de control, mínima, con `LIMIT 1`:

```
SELECT * WHERE { ?s ?p ?o } LIMIT 1
```

Resolvió el **primer** candidato probado, en **1 llamada**:

| Aspecto | Medido |
|---|---|
| URL | `https://datos.bcn.cl/sparql` |
| Código HTTP | **200** |
| `content-type` declarado | `application/sparql-results+json` |
| Bytes | 401 |
| Parsea como JSON / XML | **sí** / no |
| Llaves de primer nivel | `head`, `results` |
| Variables en `head.vars` | `s`, `p`, `o` — las tres de la consulta |
| Filas en `results.bindings` | **1**, como pidió el `LIMIT 1` |
| Persistido | sí → `g2_sparql_datos_bcn.txt` |

**Un 200 con cuerpo no se aceptó como respuesta afirmativa**: la página HTML del
buscador también devuelve 200. La compuerta exigió **forma de resultado SPARQL**
(llaves `head` y `results` en JSON, o raíz `sparql` en XML) y solo entonces
declaró la rama abierta.

El cuerpo delata el motor: el primer `binding` apunta a
`http://www.openlinksw.com/virtrdf-data-formats#default-iid`, es decir **Virtuoso**.
Dato operativo, no cosmético: fija la sintaxis disponible para M1 y G5.

**Lo que G2 NO establece, y hay que decirlo antes de que se cuele como premisa:**
que el endpoint responda no dice nada sobre si modela proyectos de ley, ni sobre
si existe una propiedad que ligue proyecto con descriptor temático. Eso es H5 y
se mide en **G5**, con los 5 boletines con materia como control positivo.

---

## G3 — ¿Responde LeyChile? — RAMA ABIERTA

H3 no era premisa. Descarga de control por identificador. **El identificador se
declaró como hipótesis y lo verificó la propia respuesta**: no se afirmó de
memoria qué norma era `idNorma=242302`; se pidió y se leyó del cuerpo.

Resolvió el **primer** candidato probado, en **1 llamada**:

| Aspecto | Medido |
|---|---|
| URL | `https://www.bcn.cl/leychile/consulta/obtxml?opt=7&idNorma=242302` |
| Código HTTP | **200** |
| `content-type` declarado | `text/xml; charset=utf-8` |
| Bytes | 561 407 |
| Parsea como XML | **sí** |
| **Nombre de la raíz** | **`Norma`** |
| Espacio de nombres / esquema | `http://www.leychile.cl/esquemas` / `EsquemaIntercambioNorma-v1-0.xsd` |
| Hijos de la raíz | `Identificador`, `Metadatos`, `Encabezado`, `EstructurasFuncionales`, `Promulgacion` |
| Persistido | sí → `g3_leychile_bcn_obtxml_242302.xml` |

**El identificador era real y la respuesta lo prueba:** `<TituloNorma>` devolvió
*"FIJA EL TEXTO REFUNDIDO, COORDINADO Y SISTEMATIZADO DE LA CONSTITUCION POLITICA
DE LA REPUBLICA DE CHILE"*, con `fechaPromulgacion 2005-09-17` y
`fechaPublicacion 2005-09-22` como atributos de `<Identificador>`.

**Dos observaciones que condicionan G4 y M4, registradas ahora para que no se
inventen después:**

1. El XML se organiza por **norma publicada**, no por proyecto en tramitación: la
   raíz es `Norma` y los hijos son estructura de texto legal
   (`EstructurasFuncionales`, `Promulgacion`). No apareció ningún nodo de boletín
   en el primer nivel. Es consistente con H4 —el techo de LeyChile es el
   subconjunto que llegó a ser ley— pero **H4 sigue sin medirse**: eso es G4.
2. Hay un bloque `<Metadatos>` cuya composición todavía **no se ha inspeccionado**.
   Si allí vive algún descriptor temático, es candidato de M4, y habrá que
   determinar si es materia legislativa o **otra cosa** (clasificación de
   ministerio, organismo emisor, tipo de norma). Un campo con nombre parecido no
   es el mismo campo.

---

## Presupuesto de red y manifiesto

| Magnitud | Valor |
|---|---|
| Tope declarado (§2) | 500 llamadas |
| Ejecutadas hasta el cierre de G3 | **2** |
| Disponibles | **498** |
| Filas en `_manifiesto_p68.csv` | **2** |
| Contador del reproductor | **2** — coincide (C4 en verde hasta aquí) |
| Respuestas no-200 | **0** |
| Archivos persistidos | **2**, ambos HTTP 200 con cuerpo no vacío (C3 en verde hasta aquí) |

Contenido íntegro del manifiesto a esta altura:

| n | fase | operación | http | bytes | persistido | archivo |
|---|---|---|---|---|---|---|
| 1 | g2 | `sparql_datos_bcn` | 200 | 401 | sí | `g2_sparql_datos_bcn.txt` |
| 2 | g3 | `leychile_bcn_obtxml` | 200 | 561 407 | sí | `g3_leychile_bcn_obtxml_242302.xml` |

Artefactos en `20_insumos/exploracion/20260813/`: el reproductor, el manifiesto y
esas 2 respuestas. **4 archivos, ninguno más.** `git status --porcelain` de la
rama: vacío.

---

## Pendiente al detenerse

1. **G4 en adelante**, a la espera de autorización. Con el umbral corregido: dos
   fracciones separadas, sobre los 422 y sobre los 336 de la cohorte 2026.
2. **Decisión del titular sobre el intermedio desalineado.** El sondeo no lo
   necesita: G1 ya midió el universo vigente desde la captura cruda, y las fases
   siguientes trabajan sobre esa lista de 422 boletines. Pero `40_salidas/intermedios/`
   sigue sellado al 2026-08-03, así que **cualquier corrida de `39` fallaría hoy**
   en `validar_corte()`. Arreglarlo es del pipeline, no de P-68.
